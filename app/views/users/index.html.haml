%h1 Użytkownicy

%table{ style: "width: 100%" }
  %tr
    %th Imie i nazwisko
    %th Profil
    %th Rola
    %th

  - @users.each do |user|
    %tr
      %td= link_to user.username, edit_user_path(user)
      %td= user.fb_id
      %td= user.role
      %td= link_to 'Usuń', user, :confirm => 'Czy na pewno?', :method => :delete

= paginate @users, :window => 10
