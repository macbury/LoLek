= form_for @user do |f|
  -if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :username
    = f.text_field :username
  .field
    = f.label :role
    = f.select :role, [[User::Admin, "Admin"], [User::Moderator, "Moderator"], [User::Normal, "Użytkownik"], [User::Bot, "Bot"]]
  .actions
    = f.submit 'Zapisz'
